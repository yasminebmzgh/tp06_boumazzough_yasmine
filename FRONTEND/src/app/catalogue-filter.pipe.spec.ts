import { CatalogueFilterPipe } from './catalogue-filter.pipe';

describe('CatalogueFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CatalogueFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
